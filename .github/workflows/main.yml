name: Auto-Deploy Flask App

on:
  push:
    branches:
      - main  # Run on pushes to the main branch

jobs:
  deploy:
    runs-on: [self-hosted]

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          path: /home/githubrunner/actions-runner/_work/Maze-Solver/Maze-Solver

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          cd /home/githubrunner/actions-runner/_work/Maze-Solver/Maze-Solver
          pip install -r requirements.txt

      - name: Start Flask App
        run: |
          cd /home/githubrunner/actions-runner/_work/Maze-Solver/Maze-Solver
          nohup python app.py &

      - name: Output Deployment URL
        run: |
          cd /home/githubrunner/actions-runner/_work/Maze-Solver/Maze-Solver
          echo "App deployed at http://your-server-ip:5000" | tee url.txt
